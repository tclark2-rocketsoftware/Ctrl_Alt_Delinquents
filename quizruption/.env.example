###############################################
# Quizruption Backend Environment Example
# Copy to .env and adjust for your deployment.
###############################################

# Network configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Security / JWT
SECRET_KEY=replace-with-secure-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS origins: comma-separated list or * to allow all
CORS_ORIGINS=*

# Database (override default SQLite path if desired)
# SQLITE_URL=sqlite:///./quizruption.db

# OpenAI key for joke generation (optional)
# OPENAI_API_KEY=sk-your-key

###############################################
# Notes:
# - All variables are loaded automatically by python-dotenv via load_dotenv() in app.main.
# - For production set a strong SECRET_KEY (32+ random bytes in hex/base64).
# - Restrict CORS_ORIGINS to your frontend domains (e.g. https://quizruption.example.com).
###############################################
# Environment variables for Quizruption Backend

# Database
DATABASE_URL=sqlite:///./quizruption.db

# Security
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# External APIs (Optional)
# GIPHY_API_KEY=your-giphy-api-key
# OPENAI_API_KEY=your-openai-api-key
