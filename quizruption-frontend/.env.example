# Option A: Direct full API URL (overrides host/port variables if present)
REACT_APP_API_URL=http://localhost:8000/api

# Option B: Compose from host/port/path (ignored if REACT_APP_API_URL is set)
# REACT_APP_API_HOST=127.0.0.1
# REACT_APP_API_PORT=8000
# REACT_APP_API_PATH=/api

# Frontend dev server customization (Create React App recognizes PORT; HOST is optional)
# PORT=3000
# HOST=0.0.0.0
